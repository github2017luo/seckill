spring:
  application:
    name: server

server:
  port: 9000

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: "http://localhost:9000/eureka/"
    # 不获取服务信息
    fetch-registry: false
    # 不注册
    register-with-eureka: false